box: wercker/default
build:
  steps:
    - arjen/hugo-build:
        version: 0.11
deploy:
  steps:
    - heroku-deploy:
        key: $HEROKU_KEY
        user: $HEROKU_USER
        app-name: $HEROKU_APP_NAME